// Placeholder — emailService.js (Brevo integration)
// Will be implemented in the auth ticket

module.exports = {
  sendEmail: async (_to, _subject, _html) => {
    throw new Error('Not implemented — see auth ticket');
  },
};
